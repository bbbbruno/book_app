<h1><%= t '.title' %></h1>

<%= render 'form', { user: @user, report: @report } %>

<%= link_to t('dictionary.button.show'), user_report_path(@user, @report) %> |
<%= link_to t('dictionary.button.back'), user_reports_path(@user) %>
