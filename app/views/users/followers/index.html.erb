<h1><%= t '.title', user: @user.username %></h1>

<ul>
  <% @users.each do |user| %>
    <%= render 'users/user', user: user %>
  <% end %>
</ul>

<%= paginate @users %>
<div class="paginate-info"><%= page_entries_info @users %></div>
